security:
  basic:
    enabled: false

management:
  security:
    enabled: false

logging:
  level:
    io:
      pivotal: DEBUG

spring:
  cloud:
    services:
      registrationMethod: direct

ribbon:
  IsSecure: false